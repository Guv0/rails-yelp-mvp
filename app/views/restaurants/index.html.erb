<h1>Restaurants</h1>
<%= link_to "Add a new restaurant", new_restaurant_path %>

<% @restaurants.each do |r|%>
 <h3><%= link_to r.name , restaurant_path(r.id) %></h3>
  <%= link_to "Edit", edit_restaurant_path(r.id) %>
  <%= link_to "Delete", restaurant_path(r), method: :delete %>
<%end%>
<br>
<%= link_to "Add a new restaurant", new_restaurant_path %>
